Para visualizar o histórico de commits, use o comando log
```sh
git log
```

Ver o log em uma linha pode ser útil quando possui muitos commits
```sh
git log --pretty=oneline
```

Para sair aperte a tecla `q`
